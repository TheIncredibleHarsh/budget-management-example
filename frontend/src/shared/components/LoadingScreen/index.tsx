import { LoadingScreenContainer } from "./Styles";

const LoadingScreen = () => {
    return (
        <LoadingScreenContainer>
            {'Loading...'}
        </LoadingScreenContainer>
    )
}

export default LoadingScreen;